#!/usr/bin/env bash
set -euo pipefail

printf "Workspace name: "
IFS= read -r label

if [[ -z "${label//[[:space:]]/}" ]]; then
  exit 0
fi

"$HOME/.config/herdr/herdr-json" workspace create --label "$label" --focus >/dev/null
